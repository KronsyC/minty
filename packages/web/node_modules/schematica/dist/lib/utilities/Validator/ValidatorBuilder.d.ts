import { GenericSchema, BooleanSchema, NumberSchema, StringSchema, AnySchema, ObjectSchema, ArraySchema } from './../../Schemas';
export default class ValidatorBuilder {
    buildStringValidator(schema: StringSchema, varname: string): Function;
    buildNumberValidator(schema: NumberSchema, _varname: string): Function;
    buildBooleanValidator(schema: BooleanSchema): Function;
    buildAnyValidator(schema: AnySchema): Function;
    buildObjectValidator(schema: ObjectSchema): Function;
    buildArrayValidator(schema: ArraySchema): Function;
    build(schema: GenericSchema, varname?: string): Function;
}
