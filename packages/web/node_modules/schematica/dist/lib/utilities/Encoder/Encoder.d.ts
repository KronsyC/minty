import Validator from "../Validator/Validator";
import { GenericSchema } from "../../Schemas";
interface EncoderOpts {
    validator?: Validator;
}
export default class Encoder {
    private validator;
    private encoderBuilder;
    constructor(opts?: EncoderOpts);
    build(schema: GenericSchema): ((data: string) => string) | ((data: number) => string) | ((data: boolean) => string) | ((data: any[]) => string);
}
export {};
