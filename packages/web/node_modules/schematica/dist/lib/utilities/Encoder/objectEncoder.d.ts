import ValidatorBuilder from '../Validator/ValidatorBuilder';
import { ObjectSchema } from './../../Schemas';
import EncoderBuilder from './EncoderBuilder';
export default function objectEncoder(schema: ObjectSchema, validatorBuilder: ValidatorBuilder, encoderBuilder: EncoderBuilder, isChild?: boolean): (data: unknown) => string;
