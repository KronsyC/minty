import { GenericSchema } from '../../Schemas';
import Validator from "../Validator/Validator";
export interface ParserParams {
    validator?: Validator;
}
export default class Parser {
    private validator;
    constructor(params: ParserParams);
    scrub(data: Object): void;
    build(schema: GenericSchema): (data: string, reviver?: ((key: string, value: any) => any) | undefined) => any;
}
