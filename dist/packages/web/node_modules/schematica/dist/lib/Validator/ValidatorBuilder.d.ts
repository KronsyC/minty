import { GenericSchema, BooleanSchema, NumberSchema, StringSchema, AnySchema, ObjectSchema, ArraySchema } from '../../Schemas';
interface BuildValidatorOptions {
    isChild?: boolean;
    context?: string;
    asFunction?: boolean;
}
export default class ValidatorBuilder {
    buildStringValidator(schema: StringSchema): string;
    buildNumberValidator(schema: NumberSchema): string;
    buildBooleanValidator(schema: BooleanSchema): string;
    buildAnyValidator(schema: AnySchema): string;
    buildObjectValidator(schema: ObjectSchema): string;
    buildArrayValidator(schema: ArraySchema): string;
    private buildDependencies;
    build(schema: GenericSchema, options?: BuildValidatorOptions): Function | string;
}
export {};
