export declare type SchemaType = "string" | "object" | "number" | "boolean" | "array" | "any";
export interface BaseSchemaTemplate {
    type: SchemaType;
    ref?: string;
    name?: string;
    nullable?: boolean;
}
export declare abstract class BaseSchema<T extends BaseSchemaTemplate> {
    type: SchemaType;
    id: string;
    name: string;
    ref?: string;
    template: T;
    cache: Map<string, any>;
    nullable: boolean;
    abstract typecheck: string;
    constructor(template: T);
    validateSchema(): void;
}
